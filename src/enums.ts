export enum DialogResponseTypes {
  ERROR = 'error',
  CREATE = 'create',
  UPDATE = 'update',
  DELETE = 'delete',
  INFO = 'info',
  SUCCESS = 'success',
  WARNING = 'warning',
  CONFIRMATION = 'confirmation',
  DEFAULT = '',
}

export enum InputType {
  TEXT = 'text',
  NUMBER = 'number',
  DROPDOWN = 'dropdown',
  DATE = 'date',
  EMAIL = 'email',
  PHONE = 'phone',
  COLOR = 'color',
}
